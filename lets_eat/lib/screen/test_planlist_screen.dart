import 'package:flutter/material.dart';

class Test_Planlist_Screen extends StatelessWidget {
  const Test_Planlist_Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('test planlist screen'),
    );
  }
}
